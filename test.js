/*let sum = [];

for (let i = 10; i >= sum.length; i--) {
 sum -= i;
}

console.log(sum);

function sumNums(num) {
  let sum = 0;
  for (let i = 1; i <= num; i++) {
    sum += i;
  }
  return sum;
}

console.log(sumNums(3)); // Outputs 6*/

let sum = 0, num;

  for (let i = 1; i <= num; i++) {
    sum += i;
  }
  return sum;